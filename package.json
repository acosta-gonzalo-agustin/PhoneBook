{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "this is the backend for the phonebook app",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "shx rm -rf dist && cd \"phonebook-front\" && npm install && npm run build && shx cp -r dist ../",
    "deploy:full": "npm run build:ui && git add . && git commit -m 'build frontend' && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "shx": "^0.4.0"
  }
}
